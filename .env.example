# URL base del backend (API Django).
# En desarrollo local puede quedar como http://localhost:8000.
# En producción DEBE ser la URL HTTPS del backend (CloudFront sirve
# el frontend por HTTPS y el navegador bloqueará peticiones a http://
# por contenido mixto).
VITE_API_URL=http://localhost:8000
